name: Deploy to Staging
on:
  push:
    branches:
      - 'main'
jobs:
  deploy:
    # needs: test
    uses: ./.github/workflows/vercel_deploy.yml
    with:
      production: false
      aliases: |
        metadata.stage.datacite.org
    secrets: inherit
